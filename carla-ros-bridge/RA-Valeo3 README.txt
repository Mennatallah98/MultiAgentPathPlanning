This is Carla ROS bridge it has several packages including those covered in the report
All the nodes have been edited to run with python 3 instead of python alot of customed edits have been made to fit case in order to run the bridge with Carla from source 

There are 2 packages added to it that contains the edited codes 

First: connections --> which conatins the servers and the clients needed to integrate with other moodules

second: mapp --> which conatins all the custom nodes created to satisfy the requiremnts of the project needs

There are also 2 packages that have nodes added and edited (the packages mentioned in the report)

carla_waypoint_publisher --> a node is added to provide all possible paths
carla_ad_agent --> the local planner is edited to take from mapp instead of carla_waypoint_publisher





 
